# This is default sample specification
# Feel free to change it
# Call ecpdk showdoc pluginspec to see the list of available fields and their description
pluginName: TestE2EPlugin
description: Test E2E description
version: 1.0.0
author: Test Pavel Vovk
authorUrl: [indefined]
category: TestE2ECategory

# This is a declaration for the plugin configuration
configuration:
  # Shell to be used for checking connection
  shell: ec-perl
  parameters:
   - name: config
     type: entry
     label: Configuration Name
     required: true
     documentation: The name for the created configuration
   - name: desc
     type: entry
     label: Description
     required: false
     documentation: Description for the configuration
   - name: endpoint
     label: Endpoint
     type: entry
     required: true
     documentation: Third-party endpoint to connect to.
   - name: credential
     type: credential
     label: Credential
     userNameLabel: Username
     required: true
     passwordLabel: Password
   - name: checkConnection
     label: Check Connection?
     type: checkbox
     checkedValue: true
     uncheckedValue: false
     initiallyChecked: 1
   - name: debugLevel
     # This parameter name "debugLevel" is supported by ECPDF Logger out of the box
     label: Debug Level
     type: select
     required: 0
     value: 1
     options:
       - name: Info
         value: 0
       - name: Debug
         value: 1
       - name: Trace
         value: 2

procedures:
- name: Get Test As JSON to TestRail
  description: The procedure take a xml and import it to Test Rail as Test Case
  hasConfig: true # configuration field will be generated automatically
  parameters:
  - name: caseId
    documentation: Target Case Id in the Test Rail for Update or Create
    type: entry
    required: true
    label: Case Id in the Test Rail
  outputParameters:
    response: Responce of TestRail
    caseJSON: case as JSON
    caseId: Id of created/updated test case on TestRail
  # Steps are not defined so we assume that this is one-step procedure with a single step named Deploy
  shell: ec-perl
- name: Import Test As JSON to TestRail
  description: The procedure take a xml and import it to Test Rail as Test Case
  hasConfig: true # configuration field will be generated automatically
  parameters:
  - name: json
    documentation: JSON of the Test Case
    type: entry
    required: true
    label: JSON Value
  - name: caseId
    documentation: Target Case Id in the Test Rail for Update or Create
    type: entry
    required: true
    label: Case Id in the Test Rail
  outputParameters:
    response: Responce of TestRail
    caseId: Id of created/updated test case on TestRail
  # Steps are not defined so we assume that this is one-step procedure with a single step named Deploy
  shell: ec-perl
